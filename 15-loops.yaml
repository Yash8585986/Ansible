- name: "demo on loops"
  hosts: local
  connection: local
  tasks:
    - name: "print loops"
      ansbible.builtin.debug:
        msg : "Hello {{ item }}"
      loop:
      - ram
      - ramesh
      - robert