- name: install packages using loops
  hosts: local
  connection: local
  became: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
      - mysql
      - nginx
      - zip
