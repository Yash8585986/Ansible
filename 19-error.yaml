- name: "Demo on error handling"
  hosts: local
  connection: local
  tasks:
    - name: "error handling"
      ansible.builtin.command: id roboshop
      register: id_output
   
    - name: "print output"
      ansible.builtin.debug:
        msg: "{{ id_output }}"
