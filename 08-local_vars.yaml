- name: play level variables
  hosts: local
  connection: local
  vars:
  
  tasks:
  - name: print variables
    vars: 
      COURSE: "Ansible"
    ansible.builtin.debug:
      msg: "Course is: {{ COURSE }}"